<ui:FluentWindow x:Class="SlipSnap.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:models="clr-namespace:SlipSnap.Models"
        Title="SlipSnap Settings"
        Width="520" MinHeight="400"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Title="SlipSnap Settings" Grid.Row="0" />

        <ScrollViewer Grid.Row="1" Margin="16,0,16,16" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Theme -->
                <TextBlock Text="Theme" FontWeight="SemiBold" FontSize="16" Margin="0,8,0,8" />
                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                    <RadioButton Content="Auto" GroupName="Theme"
                                 IsChecked="{Binding Theme, Converter={StaticResource EnumBoolConverter}, ConverterParameter={x:Static models:ThemeMode.Auto}}" Margin="0,0,12,0" />
                    <RadioButton Content="Light" GroupName="Theme"
                                 IsChecked="{Binding Theme, Converter={StaticResource EnumBoolConverter}, ConverterParameter={x:Static models:ThemeMode.Light}}" Margin="0,0,12,0" />
                    <RadioButton Content="Dark" GroupName="Theme"
                                 IsChecked="{Binding Theme, Converter={StaticResource EnumBoolConverter}, ConverterParameter={x:Static models:ThemeMode.Dark}}" />
                </StackPanel>

                <!-- Transparency -->
                <TextBlock Text="Opacity" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,8" />
                <DockPanel Margin="0,0,0,16">
                    <TextBlock DockPanel.Dock="Right" Margin="8,0,0,0" VerticalAlignment="Center">
                        <Run Text="{Binding OpacityPercent, Mode=OneWay}" /><Run Text="%" />
                    </TextBlock>
                    <Slider Minimum="10" Maximum="100"
                            Value="{Binding OpacityPercent, Mode=TwoWay}" TickFrequency="10" IsSnapToTickEnabled="True" />
                </DockPanel>

                <!-- Toolbar Edges -->
                <TextBlock Text="Toolbar Positions" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,8" />

                <CheckBox Content="Left edge" IsChecked="{Binding IsLeftEnabled}" Margin="0,0,0,2" />
                <UniformGrid Columns="2" Margin="24,0,0,8" Visibility="{Binding IsLeftEnabled, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <CheckBox Content="Start Menu" IsChecked="{Binding LeftButtons.HasStartMenu}" Margin="0,0,8,4" />
                    <CheckBox Content="Task View" IsChecked="{Binding LeftButtons.HasTaskView}" Margin="0,0,8,4" />
                    <CheckBox Content="Prev Desktop" IsChecked="{Binding LeftButtons.HasPrevDesktop}" Margin="0,0,8,4" />
                    <CheckBox Content="Next Desktop" IsChecked="{Binding LeftButtons.HasNextDesktop}" Margin="0,0,8,4" />
                </UniformGrid>

                <CheckBox Content="Right edge" IsChecked="{Binding IsRightEnabled}" Margin="0,0,0,2" />
                <UniformGrid Columns="2" Margin="24,0,0,8" Visibility="{Binding IsRightEnabled, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <CheckBox Content="Start Menu" IsChecked="{Binding RightButtons.HasStartMenu}" Margin="0,0,8,4" />
                    <CheckBox Content="Task View" IsChecked="{Binding RightButtons.HasTaskView}" Margin="0,0,8,4" />
                    <CheckBox Content="Prev Desktop" IsChecked="{Binding RightButtons.HasPrevDesktop}" Margin="0,0,8,4" />
                    <CheckBox Content="Next Desktop" IsChecked="{Binding RightButtons.HasNextDesktop}" Margin="0,0,8,4" />
                </UniformGrid>

                <CheckBox Content="Top edge" IsChecked="{Binding IsTopEnabled}" Margin="0,0,0,2" />
                <UniformGrid Columns="2" Margin="24,0,0,8" Visibility="{Binding IsTopEnabled, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <CheckBox Content="Start Menu" IsChecked="{Binding TopButtons.HasStartMenu}" Margin="0,0,8,4" />
                    <CheckBox Content="Task View" IsChecked="{Binding TopButtons.HasTaskView}" Margin="0,0,8,4" />
                    <CheckBox Content="Prev Desktop" IsChecked="{Binding TopButtons.HasPrevDesktop}" Margin="0,0,8,4" />
                    <CheckBox Content="Next Desktop" IsChecked="{Binding TopButtons.HasNextDesktop}" Margin="0,0,8,4" />
                </UniformGrid>

                <CheckBox Content="Bottom edge" IsChecked="{Binding IsBottomEnabled}" Margin="0,0,0,2" />
                <UniformGrid Columns="2" Margin="24,0,0,16" Visibility="{Binding IsBottomEnabled, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <CheckBox Content="Start Menu" IsChecked="{Binding BottomButtons.HasStartMenu}" Margin="0,0,8,4" />
                    <CheckBox Content="Task View" IsChecked="{Binding BottomButtons.HasTaskView}" Margin="0,0,8,4" />
                    <CheckBox Content="Prev Desktop" IsChecked="{Binding BottomButtons.HasPrevDesktop}" Margin="0,0,8,4" />
                    <CheckBox Content="Next Desktop" IsChecked="{Binding BottomButtons.HasNextDesktop}" Margin="0,0,8,4" />
                </UniformGrid>

                <!-- Behavior -->
                <TextBlock Text="Behavior" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,8" />
                <CheckBox Content="Only show when a fullscreen window is present"
                          IsChecked="{Binding FullscreenOnly}" Margin="0,0,0,4" />
                <CheckBox Content="Hide toolbars in RDP session"
                          IsChecked="{Binding HideInRdpSession}" Margin="0,0,0,16" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</ui:FluentWindow>
